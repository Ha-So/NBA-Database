/* Name: Haris Sohail
* Date: 11/26/2017
*  NBA Database Project  
* 
*/

//Selects the current team by id to pre-populate the update page
function selectTeam1(id){
    $("#team-selector1").val(id);
}

//Selects the draft team by id to pre-populate the update page
function selectTeam2(id){
    $("#team-selector2").val(id);
}

//Selects the current position by id to pre-populate the update page
function selectTeam3(id){
    $("#team-selector3").val(id);
}